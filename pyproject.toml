[project]
name = "remind-me-app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "alembic",
    "kivy>=2.3.1",
    "kivymd",
    "pydantic",
    "python-dotenv",
    "sqlalchemy",
]

[tool.uv.sources]
kivymd = { url = "https://github.com/kivymd/KivyMD/archive/master.zip" }
